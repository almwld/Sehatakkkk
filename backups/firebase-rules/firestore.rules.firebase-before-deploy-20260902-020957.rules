// ===== firestore.rules =====
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ✅ السماح بقراءة وكتابة جميع البيانات (للاختبار)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
