// Single source of truth for patient notifications.
// The Firestore-backed implementation lives in the shared screen so all
// notification entry points show the same real user data.
export '../shared/notifications_screen.dart';
