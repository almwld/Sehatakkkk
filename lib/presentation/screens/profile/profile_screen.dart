import 'package:flutter/material.dart';
import 'package:sehatak/presentation/screens/patient/patient_profile.dart';

/// Compatibility entry point kept for existing settings/navigation references.
/// PatientProfile is the single canonical public user profile.
class ProfileScreen extends PatientProfile {
  const ProfileScreen({super.key});
}
