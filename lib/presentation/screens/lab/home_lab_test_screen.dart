import 'package:flutter/material.dart';
class HomeLabTestScreen extends StatelessWidget { const HomeLabTestScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('فحص مخبري منزلي')), body: const Center(child: Text('احجز فحصاً مخبرياً من المنزل'))); }
