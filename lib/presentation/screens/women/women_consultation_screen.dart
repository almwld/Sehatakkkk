import 'package:flutter/material.dart';import 'package:firebase_auth/firebase_auth.dart';import 'package:sehatak/core/services/women_service.dart';class WomenConsultationScreen extends StatefulWidget{const WomenConsultationScreen({super.key});@override State<WomenConsultationScreen>createState()=>_S();}class _S extends State<WomenConsultationScreen>{final q=TextEditingController();bool busy=false;Future<void>send()async{final u=FirebaseAuth.instance.currentUser;if(u==null){ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content:Text('سجل الدخول أولاً')));return;}if(q.text.trim().isEmpty)return;setState(()=>busy=true);try{await WomenService().createConsultation(userId:u.uid,question:q.text.trim());q.clear();if(mounted)ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content:Text('تم إرسال الاستشارة')));}finally{if(mounted)setState(()=>busy=false);}}@override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('استشارة صحة المرأة')),body:Padding(padding:const EdgeInsets.all(20),child:Column(children:[TextField(controller:q,maxLines:6,decoration:const InputDecoration(labelText:'اكتبي سؤالك',border:OutlineInputBorder())),const SizedBox(height:16),SizedBox(width:double.infinity,child:ElevatedButton(onPressed:busy?null:send,child:Text(busy?'جارٍ الإرسال...':'إرسال الاستشارة')))])));}}
