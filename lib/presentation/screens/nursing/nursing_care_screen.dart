import 'package:flutter/material.dart';
class NursingCareScreen extends StatelessWidget { const NursingCareScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('تمريض منزلي')), body: const Center(child: Text('خدمة التمريض المنزلي'))); }
