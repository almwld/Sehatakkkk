# one-time repair workflow
name: Repair generated Flutter syntax
on:
  push:
    branches: [master]
permissions:
  contents: write
jobs:
  repair:
    if: ${{ !startsWith(github.event.head_commit.message, 'ci: repair generated syntax') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repair generated files
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          def edit(path, fn):
              p=Path(path); s=fn(p.read_text()); p.write_text(s)
          edit('lib/presentation/widgets/common/custom_bottom_navigation_bar.dart', lambda s:s.replace('AppImages.servicesMedicalRecords','AppImages.servicesHealthTips'))
          edit('lib/core/services/community_share_service.dart', lambda s:s.replace("import 'dart:ui' as ui;", "import 'dart:ui' as ui;\nimport 'package:flutter/rendering.dart';"))
          edit('lib/presentation/screens/dashboard/role_based_dashboard_screen.dart', lambda s:s.replace("Text(subtitle, style: TextStyle(fontSize: 10, color: Colors.grey[600]))]))]));","Text(subtitle, style: TextStyle(fontSize: 10, color: Colors.grey[600]))]))));"))
          def bottom(s):
              a=s.index('  Widget _buildSpecialChatButton'); b=s.index('\n  void _handleTap',a)
              return s[:a]+'''  Widget _buildSpecialChatButton(NavItem item, bool dark) {\n    final selected=currentIndex==item.index;\n    return GestureDetector(onTap:()=>_handleTap(item),behavior:HitTestBehavior.opaque,child:SizedBox(width:60,height:72,child:Column(mainAxisAlignment:MainAxisAlignment.end,children:[Transform.translate(offset:const Offset(0,-18),child:Container(width:56,height:56,decoration:BoxDecoration(gradient:const LinearGradient(colors:[AppColors.primary,AppColors.primaryDark]),shape:BoxShape.circle,border:Border.all(color:dark?const Color(0xFF1E293B):Colors.white,width:3)),child:Center(child:_image(item.iconPath,size:28,color:Colors.white)))),Text(item.label,style:TextStyle(fontSize:9,fontWeight:selected?FontWeight.w700:FontWeight.w500,color:selected?AppColors.primary:(dark?Colors.grey.shade400:Colors.grey.shade500))),const SizedBox(height:4)])));\n  }\n'''+s[b:]
          edit('lib/presentation/widgets/common/custom_bottom_navigation_bar.dart',bottom)
          def home(s):
              if "import 'package:flutter/rendering.dart';" not in s:s=s.replace("import 'package:flutter/material.dart';","import 'package:flutter/material.dart';\nimport 'package:flutter/rendering.dart';")
              s=s.replace('late Future<List<ProductModel>> _products;','late Future<List<ProductModel>> _productsFuture;').replace('late Future<Map<String, dynamic>> _weather;','late Future<Map<String, dynamic>> _weatherFuture;').replace('_products = PharmacyService().getAllProducts();','_productsFuture = PharmacyService().getAllProducts();').replace('_weather = _loadWeather();','_weatherFuture = _loadWeather();').replace('future: _products,','future: _productsFuture,').replace('future: _weather,','future: _weatherFuture,')
              a=s.index('  Widget _doctors('); b=s.index('  Widget _community',a)
              block='''  Widget _doctors(HomeState state,bool dark){if(state.doctors.isEmpty)return _section('أفضل الأطباء',dark,_empty('لا يوجد أطباء موثقون متاحون حالياً',dark));return _section('أفضل الأطباء',dark,SizedBox(height:210,child:ListView.separated(scrollDirection:Axis.horizontal,padding:const EdgeInsets.symmetric(horizontal:16),itemCount:state.doctors.take(6).length,separatorBuilder:(_,__)=>const SizedBox(width:10),itemBuilder:(_,i){final d=state.doctors[i];final id=(d['id']??d['doctorId']??'').toString();return InkWell(onTap:id.isEmpty?null:()=>_go('/doctor/$id'),child:Container(width:155,padding:const EdgeInsets.all(9),decoration:BoxDecoration(color:dark?_darkCard:Colors.white,borderRadius:BorderRadius.circular(16)),child:Column(children:[Expanded(child:AppImage(imageUrl:(d['photoUrl']??d['image']??ImageKit.doctor1).toString(),width:double.infinity,fit:BoxFit.cover,borderRadius:BorderRadius.circular(12))),const SizedBox(height:6),Text((d['name']??'طبيب').toString(),maxLines:1,overflow:TextOverflow.ellipsis,style:TextStyle(fontWeight:FontWeight.w900,color:dark?Colors.white:_text)),Text((d['specialty']??'تخصص طبي').toString(),maxLines:1,overflow:TextOverflow.ellipsis,style:const TextStyle(fontSize:9,color:AppColors.primary))])));}))));}\n  Widget _products(bool dark)=>_section('منتجات الصيدلية',dark,FutureBuilder<List<ProductModel>>(future:_productsFuture,builder:(_,snap){if(snap.connectionState==ConnectionState.waiting)return const SizedBox(height:200,child:Center(child:CircularProgressIndicator()));final ps=snap.data??const <ProductModel>[];if(ps.isEmpty)return _empty('لا توجد منتجات متاحة حالياً',dark);return SizedBox(height:210,child:ListView.separated(scrollDirection:Axis.horizontal,padding:const EdgeInsets.symmetric(horizontal:16),itemCount:ps.take(8).length,separatorBuilder:(_,__)=>const SizedBox(width:10),itemBuilder:(_,i){final p=ps[i];return Container(width:150,padding:const EdgeInsets.all(10),decoration:BoxDecoration(color:dark?_darkCard:Colors.white,borderRadius:BorderRadius.circular(16)),child:Column(children:[Expanded(child:AppImage(imageUrl:p.imageUrl??ImageKit.medicine1,width:90,height:90,fit:BoxFit.contain)),Text(p.name,maxLines:2,overflow:TextOverflow.ellipsis,style:TextStyle(color:dark?Colors.white:_text,fontWeight:FontWeight.w800)),Text('${p.price.toStringAsFixed(0)} ر.ي',style:const TextStyle(color:AppColors.primary,fontWeight:FontWeight.w900))]));}));}));\n  Widget _places(String title,List<Map<String,dynamic>> data,String fallback,bool dark)=>_section(title,dark,data.isEmpty?_empty('لا توجد بيانات متاحة حالياً',dark):SizedBox(height:145,child:ListView.separated(scrollDirection:Axis.horizontal,padding:const EdgeInsets.symmetric(horizontal:16),itemCount:data.take(6).length,separatorBuilder:(_,__)=>const SizedBox(width:10),itemBuilder:(_,i){final x=data[i];return Container(width:185,padding:const EdgeInsets.all(9),decoration:BoxDecoration(color:dark?_darkCard:Colors.white,borderRadius:BorderRadius.circular(16)),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Expanded(child:AppImage(imageUrl:(x['imageUrl']??x['image']??fallback).toString(),width:double.infinity,fit:BoxFit.cover,borderRadius:BorderRadius.circular(12))),Text((x['name']??'منشأة صحية').toString(),maxLines:1,overflow:TextOverflow.ellipsis,style:TextStyle(fontWeight:FontWeight.w900,color:dark?Colors.white:_text))]));})));\n  Widget _articles(List<Map<String,dynamic>> data,bool dark)=>_section('أحدث المقالات',dark,data.isEmpty?_empty('لا توجد مقالات منشورة حالياً',dark):GridView.builder(padding:const EdgeInsets.symmetric(horizontal:16),shrinkWrap:true,physics:const NeverScrollableScrollPhysics(),itemCount:data.take(4).length,gridDelegate:const SliverGridDelegateWithFixedCrossAxisCount(crossAxisCount:2,crossAxisSpacing:9,mainAxisSpacing:9,childAspectRatio:.95),itemBuilder:(_,i){final a=data[i];return Container(clipBehavior:Clip.antiAlias,decoration:BoxDecoration(color:dark?_darkCard:Colors.white,borderRadius:BorderRadius.circular(15)),child:Column(children:[Expanded(child:AppImage(imageUrl:(a['imageUrl']??a['image']??ImageKit.nutritionTips).toString(),width:double.infinity,fit:BoxFit.cover)),Text((a['title']??'مقال صحي').toString(),maxLines:2,overflow:TextOverflow.ellipsis)]));}));\n  Widget _tips(List<Map<String,dynamic>> data,bool dark)=>_section('النصائح اليومية',dark,GridView.count(crossAxisCount:2,shrinkWrap:true,physics:const NeverScrollableScrollPhysics(),padding:const EdgeInsets.symmetric(horizontal:16),children:[for(final x in ['اشرب الماء','تحرك 30 دقيقة','نم 7-8 ساعات','تناول الفواكه'])Card(color:dark?_darkCard:Colors.white,child:Center(child:Text(x,style:TextStyle(color:dark?Colors.white:_text,fontWeight:FontWeight.w800))))]));\n  void _showTip(String title,String text)=>showModalBottomSheet<void>(context:context,builder:(_)=>Padding(padding:const EdgeInsets.all(24),child:Column(mainAxisSize:MainAxisSize.min,children:[Text(title,style:const TextStyle(fontSize:20,fontWeight:FontWeight.w900)),const SizedBox(height:10),Text(text),ElevatedButton(onPressed:()=>Navigator.pop(context),child:const Text('إغلاق'))])));\n  Widget _discover(bool dark)=>_section('اكتشف المزيد',dark,const SizedBox(height:80));\n  Widget _weather(bool dark)=>_section('الطقس والتوصيات',dark,FutureBuilder<Map<String,dynamic>>(future:_weatherFuture,builder:(_,snap)=>Padding(padding:const EdgeInsets.symmetric(horizontal:16),child:Container(padding:const EdgeInsets.all(16),decoration:BoxDecoration(color:dark?_darkCard:Colors.white,borderRadius:BorderRadius.circular(18)),child:Text((snap.data?['condition']??'جاري تحديث الطقس').toString(),style:TextStyle(color:dark?Colors.white:_text,fontWeight:FontWeight.w800))))));\n'''
              s=s[:a]+block+s[b:]
              s=s.replace("Widget _section(String title, bool dark, Widget child) => Padding(padding: const EdgeInsets.only(top: 18), child: Column(children: [Padding(padding: const EdgeInsets.symmetric(horizontal: 16), child: Align(alignment: Alignment.centerRight, child: Text(title, style: TextStyle(fontSize: 17, fontWeight: FontWeight.w900, color: dark ? Colors.white : _text))), const SizedBox(height: 9), child]));","Widget _section(String title,bool dark,Widget child)=>Padding(padding:const EdgeInsets.only(top:18),child:Column(children:[Padding(padding:const EdgeInsets.symmetric(horizontal:16),child:Align(alignment:Alignment.centerRight,child:Text(title,style:TextStyle(fontSize:17,fontWeight:FontWeight.w900,color:dark?Colors.white:_text)))),const SizedBox(height:9),child]));")
              s=s.replace("Widget _empty(String text, bool dark) => Padding(padding: const EdgeInsets.symmetric(horizontal: 16), child: Container(height: 95, width: double.infinity, alignment: Alignment.center, decoration: BoxDecoration(color: dark ? _darkCard : Colors.white, borderRadius: BorderRadius.circular(16)), child: Text(text, style: TextStyle(color: dark ? Colors.white60 : _muted)));","Widget _empty(String text,bool dark)=>Padding(padding:const EdgeInsets.symmetric(horizontal:16),child:Container(height:95,width:double.infinity,alignment:Alignment.center,decoration:BoxDecoration(color:dark?_darkCard:Colors.white,borderRadius:BorderRadius.circular(16)),child:Text(text,style:TextStyle(color:dark?Colors.white60:_muted))));")
              return s
          edit('lib/presentation/screens/home/tabs/home_tab_pro.dart',home)
          def platform(s):
              a=s.index('  @override Widget build'); b=s.rindex('\n}')
              m='''  @override Widget build(BuildContext context){final dark=Theme.of(context).brightness==Brightness.dark;return Scaffold(backgroundColor:dark?const Color(0xFF0B1121):const Color(0xFFF8FAFC),appBar:CustomAppBar(title:'إدارة التوثيق',backgroundColor:AppColors.primary,foregroundColor:Colors.white,elevation:0),body:_loading?const Center(child:CircularProgressIndicator()):(_error!=null||!_isAdmin)?Center(child:Text(_error??'غير مصرح لك بالدخول')):RefreshIndicator(onRefresh:_load,child:ListView.builder(padding:const EdgeInsets.all(16),itemCount:_pending.length,itemBuilder:(c,i){final d=_pending[i].data();final id=_pending[i].id;return Card(child:ListTile(title:Text(d['name']?.toString()??'طبيب'),subtitle:Text(d['specialty']?.toString()??'غير محدد'),trailing:Wrap(children:[IconButton(onPressed:()=>_review(id,'reject'),icon:const Icon(Icons.close)),IconButton(onPressed:()=>_review(id,'approve'),icon:const Icon(Icons.check))]));})));}\n'''
              return s[:a]+m+s[b:]
          edit('lib/presentation/screens/platform/dashboard/platform_dashboard.dart',platform)
          def marketplace(s):
              a=s.index('  @override Widget build'); b=s.index('\n  Widget _section',a)
              m='''  @override Widget build(BuildContext context){return Scaffold(appBar:AppBar(title:const Text('إدارة Marketplace'),backgroundColor:AppColors.primary,foregroundColor:Colors.white,actions:[IconButton(onPressed:_load,icon:const Icon(Icons.refresh))]),body:_loading?const Center(child:CircularProgressIndicator()):_error!=null?Center(child:Text(_error!)):RefreshIndicator(onRefresh:_load,child:ListView(padding:const EdgeInsets.all(12),children:[_section('طلبات اعتماد الصيدليات',_pharmacies,(p)=>Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('${p['name']??'صيدلية'}'),Text('${p['address']??''}'),_actions(()=>_reviewPharmacy('${p['id']}','reject'),()=>_reviewPharmacy('${p['id']}','approve'))])),const SizedBox(height:16),_section('منتجات معلقة',_products,(p)=>Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('${p['name']??'منتج'}'),Text('${p['price']??0} ر.ي'),_actions(()=>_reviewProduct('${p['id']}','reject'),()=>_reviewProduct('${p['id']}','approve'))]))]));}\n'''
              return s[:a]+m+s[b:]
          edit('lib/presentation/screens/platform/marketplace_admin_dashboard.dart',marketplace)
          edit('lib/presentation/screens/subscriptions/subscriptions_screen.dart',lambda s:s.replace('_trustNote()]));','_trustNote()])));'))
          edit('lib/presentation/screens/pharmacy/cart_screen.dart',lambda s:s.replace("..._cart.items.map((item) => Card(child: ListTile(title: Text(item.name), subtitle: Text('${item.unitPrice.toStringAsFixed(0)} ر.ي × ${item.quantity}'), trailing: Row(mainAxisSize: MainAxisSize.min, children: [IconButton(onPressed: () => setState(() => _cart.decrement(item.productId)), icon: const Icon(Icons.remove_circle_outline)), Text('${item.quantity}'), IconButton(onPressed: () => setState(() => _cart.increment(item.productId)), icon: const Icon(Icons.add_circle_outline)), IconButton(onPressed: () => setState(() => _cart.remove(item.productId)), icon: const Icon(Icons.delete_outline, color: Colors.red))]))),","..._cart.items.map((item) => Card(child: ListTile(title: Text(item.name), subtitle: Text('${item.unitPrice.toStringAsFixed(0)} ر.ي × ${item.quantity}'), trailing: Row(mainAxisSize: MainAxisSize.min, children: [IconButton(onPressed: () => setState(() => _cart.decrement(item.productId)), icon: const Icon(Icons.remove_circle_outline)), Text('${item.quantity}'), IconButton(onPressed: () => setState(() => _cart.increment(item.productId)), icon: const Icon(Icons.add_circle_outline)), IconButton(onPressed: () => setState(() => _cart.remove(item.productId)), icon: const Icon(Icons.delete_outline, color: Colors.red))])))),"))
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add lib
          git diff --cached --quiet && exit 0
          git commit -m 'ci: repair generated syntax'
          git push
