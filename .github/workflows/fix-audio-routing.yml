name: Fix audio routing

# This temporary patch workflow is intentionally disabled. Audio routing is
# maintained in the application source, not by a workflow that rewrites and
# pushes source files on every commit.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary audio patch workflow disabled; source is maintained normally."
